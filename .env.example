# -----------------------------
#  DATABASE CONFIGURATION
# -----------------------------
POSTGRES_DB=mydb               # Name of the PostgreSQL database
POSTGRES_USER=myuser           # Username for the PostgreSQL database
POSTGRES_PASSWORD=mysecret      # Password for the PostgreSQL user
POSTGRES_HOST=db               # Host address for the Postgres service (in Docker, "db" is the service name)
POSTGRES_PORT=5432             # Port on which the PostgreSQL service is running

# -----------------------------
#  REDIS / CELERY CONFIGURATION
# -----------------------------
# Redis broker URL used by Celery (defaults to redis service in Docker Compose)
BROKER_URL=redis://redis:6379/0

# -----------------------------
#  APPLICATION SETTINGS
# -----------------------------
# You can add more environment variables as needed:
# e.g., APP_ENV=development, APP_NAME="My Pipeline Project", etc.
APP_ENV=development
APP_NAME="My Pipeline Project"
